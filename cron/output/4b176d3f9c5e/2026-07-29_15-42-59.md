# Cron Job: 糅合量化自进化引擎

**Job ID:** 4b176d3f9c5e
**Run Time:** 2026-07-29 15:42:59
**Schedule:** 30 15 * * 1-5

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

运行糅合量化选股的v2自进化模式，检查历史推荐的正确率，调整权重。

执行命令：
cd /home/ubuntu/Desktop/hermes/quant-skill && python3 quant_ensemble.py --evolve --days 5 2>&1

## Response

## 🧠 QuantEnsemble v2 — 自进化模式运行报告

**执行命令**: `python3 quant_ensemble.py --evolve --days 5`

---

### 运行结果：⚠️ 无有效验证数据（预期行为）

| 项目 | 状态 |
|---|---|
| 模型加载 | ✅ K线模型加载成功 (2s) |
| BaoStock 登录 | ✅ 成功 |
| Log 检测 | ✅ 发现 3 天日志 (07/27, 07/28, 07/29) |
| 历史验证 | ❌ **无有效验证数据** |
| 权重调整 | ❌ 未执行 |

### 原因分析

自进化模式需要 **推荐日距今 ≥5 个交易日** 的价格数据来计算收益率，但当前日志只有最近 3 个交易日：

| 日志日期 | 距今天数 (交易日) | 是否满足 ≥5 |
|---|---|---|
| 07/27 (Mon) | 3 天 (→07/29 Wed) | ❌ |
| 07/28 (Tue) | 2 天 | ❌ |
| 07/29 (Wed, 今天) | 0 天 | ❌ |

这是系统刚开始运行时的**正常现象**。等积累够 7 天以上的日志后，evolve 模式才能自动验证推荐质量并调整权重。

### 当前系统状态快照

| 指标 | 值 |
|---|---|
| `weights.json` | ❌ 不存在（使用默认权重）|
| 默认权重 | `tech: 0.45, kronos: 0.30, flow: 0.25` |
| 每日评分股票数 | 仅 **8 只**（CSI300 覆盖度极低）|
| **⚠️ 问题** | 所有股票的 `flow` 资金流评分均为 **0.000** |
| **⚠️ 问题** | Kronos 评分在 Top-K 中全为 **1.000**，缺乏区分度 |

### 🚨 两个需要关注的信号

1. **资金流信号全灭（flow = 0.0）** — 所有 8 只历史推荐股的 `flow` 评分都是 0。要么大单资金流数据源（BaoStock `sz.300450` 等）返回异常，要么数据处理环节出错。这会直接让权重的 25% 变成废物权重。

2. **Kronos 评分区分度低** — Top 中几乎全是 `kronos=1.0`，这表示 Kronos 模型评分缺乏差异性，无法有效排序。需要确认 Kronos 模型是否正常工作。

### 建议下一步

```
# 跑一次全量运行，观察实际输出
python3 quant_ensemble.py --full --top 20

# 检查 3 天后（等够 5 个交易日日志）再跑 evolve
# 届时将自动验证这 8 条推荐的表现，重新校准权重
```

**总结**: ✅ 脚本运行正常，但数据积累不足，自进化还动不了。目前属于冷启动阶段，预计 **下周一（08/03）** 跑 evolve 就能出有效结果。
