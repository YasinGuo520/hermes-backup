# Cron Job: AI英语每日一练

**Job ID:** 73d214f48a6b
**Run Time:** 2026-08-09 08:19:55
**Schedule:** 0 8 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

你是一个英语学习助手。今天的任务是：

1. 用web_search搜索一项今天的热门AI工具/项目/新闻（用英文搜，比如 "trending AI tool today" 或 "new AI paper github"）
2. 从搜索结果里找到一篇英文文章/README/博客
3. 用web_extract提取该页面的内容，从中截取一段2-3句的英文原文（有信息量、包含3-5个值得学的技术词）
4. 给出以下结构化的输出：

```
📖 【今日AI英语片段】
───────────────
🔤 原文
[这里放2-3句英文原文，标明出处]
───────────────
🀄 中文翻译
[整段翻译]
───────────────
📝 核心词汇

| 单词 | 词性 | 释义 | 例句 |
|------|------|------|------|
| word1 | n/v/adj | 中文释义 | 原文中用到的例子 |
| word2 | ... | ... | ... |
（列出3-5个词）
───────────────
💡 一句话用法提示
[教一个实用的造句模式，比如"你今天可以用xx句型来写Prompt"]
```

要求：
- 原文必须是今天的热门/真实内容，不编造
- 词性标记准确
- 中文翻译自然，不机器味
- 单词优先级：技术术语 > 高频动词 > 其他
- 整个输出控制在500字以内

## Response

```
📖 【今日AI英语片段】
───────────────
🔤 原文
Source: GitHub — PrimeIntellect-ai/prime-agent (README, 2026-08-08 更新)

"Prime Agent combines a persistent Python control environment with durable harness state, so useful working context and reusable operating patterns can outlive a single chat window. Everything is programmatic: persistent IPython is the built-in model tool; file operations, shell commands, tool use, subagents, and context management happen through code. Sessions run in the background: daemon-backed agents keep running when the terminal disconnects and can be reattached later."
───────────────
🀄 中文翻译
Prime Agent 把持久的 Python 控制环境与耐用的 harness（工作框架）状态结合起来，让有用的工作上下文和可复用的操作模式能活过单个聊天窗口。一切都是程序化的：常驻的 IPython 是内置的模型工具，文件操作、shell 命令、工具调用、子代理和上下文管理全部通过代码完成。会话在后台运行：守护进程支撑的代理在终端断开后仍继续跑，之后可以随时重新挂接回来。
───────────────
📝 核心词汇

| 单词 | 词性 | 释义 | 例句 |
|------|------|------|------|
| persistent | adj | 持久化的，常驻的 | a persistent Python control environment |
| harness | n | 工作框架/控制架构（AI语境） | durable harness state |
| daemon | n | 守护进程，后台服务 | daemon-backed agents |
| reattach | v | 重新连接/挂接 | can be reattached later |
| programmatic | adj | 程序化的，可编程的 | Everything is programmatic |
───────────────
💡 一句话用法提示
写工具需求/给AI下指令时，套这个句型：**"… so … can outlive a single session"**（让…超越单次会话存活）。
例：*"Save my prompts as skills, so they can outlive a single session and be reused across projects."* —— 想要 AI 记住上下文、跨会话复用，这句话直接能用。
```
