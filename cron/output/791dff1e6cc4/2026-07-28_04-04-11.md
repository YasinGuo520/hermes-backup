# Cron Job: 记忆瘦身

**Job ID:** 791dff1e6cc4
**Run Time:** 2026-07-28 04:04:11
**Schedule:** 0 4 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行记忆瘦身任务。

1. 用 memory tool 检查当前 memory 使用率（action='add' 放一个占位符然后删掉，看 usage 百分比）
2. 如果 usage > 60%（即超过 1320/2200 chars），执行清理：
   - 识别过时、重复、session-specific 的条目（已完成的一次性操作记录、过时的工具状态等）
   - 将重复/可合并的条目压缩合并
   - 优先保留：用户身份偏好、项目配置、工作流规则、成本信息
   - 优先删除：一次性操作记录（"已配置XXX"）、session-specific 的临时信息
   - 用 batch operations（operations 数组）一次性完成清理
3. 如果 usage <= 60%，什么也不用做
4. 汇报结果：清理前后 usage 对比，删/合了多少条

注意：只清理 memory（个人笔记），不动 user_profile（用户档案）。不要动技能文件。
完成后不需要通知用户（deliver='local'）。

## Response

Memory 功能在当前 cron 环境下不可用。调查详情：

**配置层面：** `config.yaml` 有 `memory` 段（`memory_char_limit: 5000`, `user_char_limit: 3000`），但 **没有配置 `provider`**。memory tool 返回明确的 "Memory is not available. It may be disabled in config or this environment."

**存储层面：** 全盘搜索确认：
- 无 `memory*.json` 文件
- 无 `.sqlite` 数据库文件
- 无 `store/` 目录
- `~/.hermes/profiles/` 为空（无其他 profile）

**结论：** 没有活跃的 memory provider → 没有需要清理的数据。

**[SILENT]** — 报告完成，无需投递。
