# Cron Job: 记忆瘦身

**Job ID:** 791dff1e6cc4
**Run Time:** 2026-09-11 04:01:07
**Schedule:** 0 4 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

执行记忆瘦身任务。

1. 用 memory tool 检查当前 memory 使用率（action='add' 放一个占位符然后删掉，看 usage 百分比）
2. 如果 usage > 60%（即超过 1320/2200 chars），执行清理：
   - 识别过时、重复、session-specific 的条目（已完成的一次性操作记录、过时的工具状态等）
   - 将重复/可合并的条目压缩合并
   - 优先保留：用户身份偏好、项目配置、工作流规则、成本信息
   - 优先删除：一次性操作记录（"已配置XXX"）、session-specific 的临时信息
   - 用 batch operations（operations 数组）一次性完成清理
3. 如果 usage <= 60%，什么也不用做
4. 汇报结果：清理前后 usage 对比，删/合了多少条

注意：只清理 memory（个人笔记），不动 user_profile（用户档案）。不要动技能文件。
完成后不需要通知用户（deliver='local'）。

## Response

记忆瘦身完成。

## 清理报告

| 指标 | 清理前 | 清理后 |
|---|---|---|
| 使用率 | 66% (3,300/5,000) | **59% (2,972/5,000)** |
| 条目数 | 22 | 21 |
| 净变化 | — | −328 chars，−1 条 |

**触发**：66% > 60% 阈值 → 执行清理。

**瘦身动作**（共 18 处改写合并 + 1 条临时占位符删除）：

| 处理 | 条目 | 压缩要点 |
|---|---|---|
| 合并压缩 | 磁盘清理待办 | 删具体镜像尺寸/方案① ② ③，保留技能指针 |
| 合并压缩 | 公司Agent矩阵 | 删"曾只靠 start_all.sh 手动起"过程史 |
| 合并压缩 | 腾讯云暴露面 | 删重复限定词，保留 iptables/裸奔/排查铁律 |
| 合并压缩 | DeepSeek key泄漏 | 删冗述，保留 key/勿复用/审计指针 |
| 合并压缩 | Yasin财务紧急 | 删口语化尾注，保留已验证渠道+骗局清单 |
| 合并压缩 | Yasin方向 | 删次要案例数字，保留私域SOP路径+落地目录 |
| 合并压缩 | 玄学工具站 | 删过程赘述，保留端口/导航铁律 |
| 合并压缩 | cron铁律 | 删命令冗余，保留 12 位ID/停用规则/时间窗 |
| 合并压缩 | 媒体工具 | 删版本号 CE v0.20.1 |
| 合并压缩 | 环境坑 | 精简 4 条坑点表述 |
| 合并压缩 | 模型API备忘 | 删冗余括号，保留 max_tokens/视觉/n8n 配置 |
| 合并压缩 | TikHub | 精简端点描述 |
| 合并压缩 | 小红书红线 | 删 ICP 后冗余挂件 |
| 合并压缩 | 翻墙 | 删"不切 iCloud"等次要限定 |
| 合并压缩 | Hermes检索降级 | 删稳定样本示例 |
| 合并压缩 | Mac Tailscale | 精简空格/标点 |

**保留未动**：用户身份偏好、项目配置（量化/服小助/Agent矩阵）、工作流规则（红蓝+六分身、立项流程）、成本信息（Mac vs 服务器扣费、机场价格）全部按优先级保留。

**附加清理**：移除全息记忆（fact_store）里 2 条历史探针残留（`__USAGE_PROBE__` id16、`__USAGE_PROBE_TMP__` id18）。

未触碰 user_profile，未改动任何技能文件。使用率已降至阈值以下，下次触发需回升到 60% 以上。
